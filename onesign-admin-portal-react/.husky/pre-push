#!/bin/sh

# Pre-push hook for additional checks

echo "🚀 Running pre-push checks..."

cd onesign-admin-portal-react

# 1. Build check
echo "\n🏗️  Building project..."
npm run build
if [ $? -ne 0 ]; then
  echo "❌ Build failed. Please fix build errors."
  exit 1
fi

# 2. Run all tests including E2E (optional - can be slow)
# echo "\n🧪 Running E2E tests..."
# npm run test:e2e
# if [ $? -ne 0 ]; then
#   echo "❌ E2E tests failed."
#   exit 1
# fi

echo "\n✅ All pre-push checks passed! Ready to push."
exit 0
