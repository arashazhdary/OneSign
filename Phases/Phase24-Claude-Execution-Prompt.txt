You are a senior software architect, staff-level backend engineer (.NET 10 / EF Core), senior frontend engineer (React / Next.js), and product-minded IAM/SSO expert.

You are working on the **onesign** platform:
- Multi-tenant SSO / IAM / IGA
- Phased roadmap: Phase 1..23 are already designed and (mostly) implemented
- Backend: .NET 10, modular monolith, clean architecture, per-module boundaries
- Frontend: React / Next.js Admin Portal + Login Portal + DevPortal + Account Center
- Multi-language (English + Persian) end-to-end

Your job now is to **analyze and implement Phase 24**.

--------------------------------------------------
High-level task
--------------------------------------------------

1) Find and deeply analyze the **Phase 24 specification** document in this workspace  
   (e.g. something like: `Phase24-*.md` under `docs/` or root).

2) Build a precise internal mental model of:
   - What Phase 24 is supposed to add to onesign
   - Which modules / layers it touches (backend, frontend, DevPortal, etc.)
   - Which cross-cutting concerns are involved (security, multi-tenant, org tree, billing, privacy, DR, crypto, …)

3) Turn the Phase 24 spec into:
   - A concrete checklist of backend changes (entities, EF mappings, services, domain logic, APIs, background jobs)
   - A concrete checklist of frontend changes (routes, pages, components, state management, i18n strings)
   - Any required docs updates (DevPortal, Admin docs, etc.)

4) Then **actually implement Phase 24** in the codebase:
   - No TODO
   - No NotImplementedException
   - No “stub for later” code
   - Production-grade, consistent with previous phases and existing conventions.

--------------------------------------------------
Global constraints (must respect all previous phases)
--------------------------------------------------

- Architecture:
  - Modular monolith, modules like:
    - `Onesign.Modules.Identity`, `Onesign.Modules.Tenants`, `Onesign.Modules.Authorization`,
      `Onesign.Modules.Federation`, `Onesign.Modules.Billing`, `Onesign.Modules.Observability`,
      `Onesign.Modules.DevPortal`, `Onesign.Modules.Governance`, `Onesign.Modules.NotificationCenter`,
      `Onesign.Modules.AccessRequests`, `Onesign.Modules.IdentityLifecycle`,
      `Onesign.Modules.PrivilegedAccess`, `Onesign.Modules.AdaptiveSecurity`,
      `Onesign.Modules.Extensibility`, `Onesign.Modules.MultiRegionDR`,
      `Onesign.Modules.Environment`, `Onesign.Modules.Crypto`, `Onesign.Modules.Privacy`, …
  - Keep Phase 24 inside the right module(s) or create a new module if the spec explicitly says so.

- Multi-tenant:
  - Tenant boundaries are strict.
  - Any new data must have clear TenantId / EnvironmentId / RegionId semantics.
  - Never “accidentally” mix tenants in queries.

- Multi-language:
  - All new UI text must go through the existing i18n system.
  - No hard-coded user-facing strings in React or backend.
  - Assume base language = English, second language = Persian (RTL).

- No TODO / no half-implementation:
  - Do not leave TODO comments or empty implementations.
  - If an endpoint or page exists, it must do real, meaningful work.
  - If the spec says “Phase 24 does X”, you either implement X or clearly wire it to already-existing infrastructure from previous phases.

- Security / compliance:
  - Respect:
    - Crypto / key management (Phase 22)
    - Privacy & DSR / retention rules (Phase 23)
    - Multi-region / DR (Phase 20)
    - On-Prem / Hybrid environments (Phase 21)
  - Any feature that touches tokens, PII, logs, or admin controls must be secure and auditable.

--------------------------------------------------
Step 1 – Analyze Phase 24 spec
--------------------------------------------------

1) Locate the Phase 24 markdown document:
   - For example: `Phase24-Onesign-*.md` under `docs/` or similar.
   - If there are multiple candidates, pick the one that clearly describes Phase 24 of the roadmap.

2) Read the Phase 24 doc **line by line**, and build a structured summary:
   - What problem does Phase 24 solve?
   - Which personas does it target? (Global Admin, Tenant Admin, DPO, Dev, End User, Security Officer, etc.)
   - What are the main epics and user stories?
   - What backend behaviors, jobs, or flows must be introduced or extended?
   - What UI surfaces must be added or modified (Admin Portal, DevPortal, Account Center, Login Portal)?

3) From the spec, extract:
   - A list of domain concepts and entities (with fields and relations)
   - A list of APIs and routes to add/extend (backend)
   - A list of UI routes, pages, and components (frontend)
   - Any cross-phase dependencies (e.g. hooks into Billing, Governance, Observability, Privacy, Crypto, DR, etc.)

4) Write (in the editor, as a comment or scratchpad) a short internal checklist for yourself:
   - “Backend tasks for Phase 24”
   - “Frontend tasks for Phase 24”
   - “Docs/tasks for Phase 24”

You don’t need to output that checklist for the user, but you must use it to drive the implementation.

--------------------------------------------------
Step 2 – Backend implementation plan (based on Phase 24)
--------------------------------------------------

Based strictly on the Phase 24 spec:

1) Design/update domain entities:
   - Add/extend entities in the right module (e.g. new `*Policy`, `*Config`, `*Event`, `*Job`, …).
   - Create EF Core mappings:
     - Proper table names (following existing naming conventions).
     - Indexes on TenantId / EnvironmentId / RegionId and other hot query fields.
     - Foreign keys consistent with earlier phases.

2) Services and application layer:
   - Add/extend interfaces and implementations in Application layer:
     - e.g. `IXXXService`, `IXXXJob`, `IXXXEvaluator`, etc.
   - Implement concrete business rules exactly as described in the Phase 24 doc.
   - Handle edge cases and error conditions; do not silently swallow failures.

3) APIs:
   - Add/extend controllers/endpoints:
     - Use the same route patterns as earlier phases (`/api/global/...`, `/api/tenant/...`, `/api/account/...`, `/api/devportal/...`).
   - Enforce authorization:
     - Global Admin vs Tenant Admin vs End User vs DevPortal clients; match Phase 24 spec.
   - Validate inputs, return proper error codes and problem details.

4) Background jobs / schedulers (if Phase 24 includes any):
   - Add jobs to the existing scheduling infrastructure.
   - Respect multi-tenant and per-environment scoping.
   - Log and audit job runs.

5) Audit & Observability hooks:
   - Emit audit events for all important state transitions (creation, update, delete, policy changes, etc.).
   - Integrate with existing Observability module if the Phase 24 spec says so.

6) Tests:
   - Add unit/integration tests where the project already has tests for earlier phases.
   - Cover main happy-path + important edge-cases defined by Phase 24.

--------------------------------------------------
Step 3 – Frontend implementation plan (Admin Portal / DevPortal / Account Center)
--------------------------------------------------

Based on the Phase 24 spec:

1) Identify which frontends are affected:
   - Admin Portal (Global / Tenant)
   - DevPortal
   - Account Center
   - Login Portal (if any UX/flow change is required).

2) For each affected area:
   - Add new routes/pages as described in Phase 24:
     - Use consistent route patterns (e.g. `/global/...`, `/tenant/...`, `/devportal/...`, `/account/...`).
   - Implement pages using existing design system:
     - React + whatever UI kit is in use (match existing components).
     - i18n-aware: all text via translation keys.
     - Handle loading, empty state, error state correctly.

3) Wire UI to backend:
   - Call the exact APIs introduced/extended for Phase 24.
   - Handle pagination, filters, sorting if required by the spec.
   - Respect role-based visibility (e.g. Global Admin-only sections, Tenant Admin-only sections, End User-only sections).

4) No dead UIs:
   - Do not add a page that is not wired to real backend logic.
   - Do not show buttons that do nothing; each action must be implemented or clearly disabled with explanation, according to spec.

--------------------------------------------------
Step 4 – Cross-phase consistency checks
--------------------------------------------------

Before you consider Phase 24 “done”, verify:

1) Multi-tenant and environment behavior:
   - All new queries and commands are correctly scoped by TenantId / EnvironmentId.
   - No leakage of one tenant’s data into another.

2) Integration with previous phases:
   - If Phase 24 interacts with:
     - Billing
     - Governance / Access Review
     - DevPortal / SDKs / API keys
     - MultiRegion / DR
     - Crypto / keys / tokens
     - Privacy / DSR / retention
   - Make sure you’re using existing public services and APIs instead of duplicating logic.

3) Security:
   - New endpoints have correct authorization attributes and permission checks.
   - No sensitive fields are exposed in list endpoints if the spec doesn’t allow it.

4) Docs:
   - If the spec requires DevPortal or admin documentation updates, add markdown docs under the appropriate `docs/` folder.
   - Keep naming and structure consistent with docs from Phases 20–23.

--------------------------------------------------
Step 5 – Concrete actions to perform now
--------------------------------------------------

1) Locate and read the Phase 24 spec markdown file in this repo.
2) Build a concise internal checklist of backend + frontend tasks from that doc.
3) Implement all backend changes (entities, EF models, services, APIs, jobs) required by Phase 24.
4) Implement all frontend changes (routes, pages, components, wiring) required by Phase 24.
5) Add/extend tests to cover the main behavior introduced in Phase 24.
6) Make sure:
   - The solution builds.
   - Existing tests pass.
   - New features behave correctly in a minimal manual test (e.g., via Swagger + Admin Portal / Account Center).
7) Clean up:
   - Remove dead code or obsolete flags if Phase 24 replaces earlier temporary behavior.

Remember:
- No TODO.
- No fake implementation.
- Respect multi-tenant, multi-region, privacy, and security constraints from earlier phases.
- Implement Phase 24 as a real, production-grade capability in the onesign platform.
